import 'package:flutter/material.dart';

class AppColors {
  // Main brand color (used for buttons, headings, active states)
  static const Color primary = Color(0xFF16767C);

  // Warm tan accent (used for icons inside input fields, small highlights)
  static const Color secondary = Color(0xFFB6AB83);

  // Darker teal accent (used for hover states, secondary emphasis)
  static const Color accent = Color(0xFF306C78);

  // Background color behind everything
  static const Color background = Color(0xFFFCFBFA);

  // White card/surface background
  static const Color surface = Color(0xFFFFFFFF);

  // Main text color (warm near-black)
  static const Color onSurface = Color(0xFF1E1D1A);

  // Muted warm gray text (subtitles, hints)
  static const Color onSurfaceVariant = Color(0xFF6B6A63);

  // Border color for input fields
  static const Color outlineVariant = Color(0xFFE3DFD3);

  // Error/red color for validation messages
  static const Color error = Color(0xFFBA1A1A);
}