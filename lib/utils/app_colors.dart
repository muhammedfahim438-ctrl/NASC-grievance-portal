import 'package:flutter/material.dart';

class AppColors {
  // Main brand color (used for buttons, headings, active states)
  static const Color primary = Color(0xFF0F766E);

  // Slightly lighter teal (used for icons inside input fields)
  static const Color secondary = Color(0xFF14B8A6);

  // Background color behind everything
  static const Color background = Color(0xFFF8FAFC);

  // White card/surface background
  static const Color surface = Color(0xFFFFFFFF);

  // Main text color (dark, almost black)
  static const Color onSurface = Color(0xFF0F172A);

  // Lighter gray text (subtitles, hints)
  static const Color onSurfaceVariant = Color(0xFF475569);

  // Border color for input fields
  static const Color outlineVariant = Color(0xFFCBD5E1);

  // Error/red color for validation messages
  static const Color error = Color(0xFFBA1A1A);
}